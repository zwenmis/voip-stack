<include>
  <gateway name="sip_trunk">
    <param name="username" value="${SIP_TRUNK_USERNAME}"/>
    <param name="password" value="${SIP_TRUNK_PASSWORD}"/>
    <param name="realm" value="${SIP_TRUNK_REALM}"/>
    <param name="proxy" value="${SIP_TRUNK_PROXY}"/>
    <param name="register" value="true"/>
    <param name="register-transport" value="tls"/>
    <param name="contact-params" value="transport=tls"/>
    <param name="caller-id-in-from" value="true"/>
    <param name="extension" value="${SIP_TRUNK_USERNAME}"/>
    <param name="extension-in-contact" value="true"/>
    <param name="retry-seconds" value="30"/>
    <param name="expire-seconds" value="300"/>
    <param name="ping" value="25"/>
    <variables>
      <variable name="sip_invite_domain" value="${SIP_TRUNK_REALM}"/>
      <variable name="sip_cid_type" value="pid"/>
    </variables>
  </gateway>
</include>